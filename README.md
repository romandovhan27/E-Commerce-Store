# E-commerce Template with React

This template is for Ecommerce site with React.

## Requirements

- Node: >= `18.x`

## Setup

```bash
npm install
```
## Run

```bash
npm run dev
```

Now you can visit template site on `http://localhost:5000`

## Build

```bash
npm run build
```

Now you can deploy the dist folder to server.